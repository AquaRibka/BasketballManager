[package]
name = "local-postgres"
version = "0.1.0"
edition = "2024"

[dependencies]
pg-embed = { version = "1", features = ["rt_tokio_migrate"] }
tokio = { version = "1", features = ["macros", "rt-multi-thread", "signal", "time"] }
